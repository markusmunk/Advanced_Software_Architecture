version: "3"

networks:
  services:
    driver: bridge
  orders:
    driver: bridge

services:
  frontend:
    image: ghcr.io/markusmunk/react_frontend-order-management:latest
    container_name: frontend
    ports:
      - "5173:5173"
    networks:
      - services

  order-management:
    image: ghcr.io/markusmunk/dotnet_order_management:latest
    container_name: order-management-service
    depends_on:
      - orders_db
    ports:
      - "5033:80"
    environment:
      - ASPNETCORE_URLS=http://+:80
    networks:
      - services
      - orders
  
  orders_db:
    image: postgres:latest
    container_name: orders_db
    environment:
      POSTGRES_DB: ordersDb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - orders_data:/var/lib/postgresql/data
    networks:
      - orders
        
volumes:
  orders_data:        